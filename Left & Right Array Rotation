//     void reverse(vector<int> &nums, int low, int high)
//     {
//             while(low < high)
//             {
//                     swap(nums[low], nums[high]);
//                     low++;
//                     high--;
//             }
//     }
        
//     void rotate(vector<int> &nums, int k) // for right rotate 
//     choose the last k numbers and first reverse them, then choose remaining numbers
//      from the start till n-k-1 and reverse them and then again reverse the whole array
//     {
//         reverse(nums,nums.size()-k,nums.size()-1);
//         reverse(nums,0,nums.size()-k-1);
//         reverse(nums, 0,nums.size()-1);
//     }
//         for left rotate 
// //     choose the first k numbers and first reverse them, then choose remaining numbers
// //      from the k+1 till n-1 and reverse them and then again reverse the whole array

//     void rotate(vector<int> &nums, int k) // for right rotate 
// //     choose the last k numbers and first reverse them, then choose remaining numbers
// //      from the start till n-k-1 and reverse them and then again reverse the whole array
// //     {
// //         reverse(nums,0,k;
// //         reverse(nums,k+1,nums.size()-1);
// //         reverse(nums, 0,nums.size()-1);
// //     }
